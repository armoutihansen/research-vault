# Literature notes (Layer 1)

One note per in-scope Zotero item: `@<citekey>.md`. Structured deep summaries written by
`/lit-sync`. Scope = items with a PDF, or tagged `to-note` (the Zotero `Obsidian scope` saved search).

Above the `%% … %%` fence is agent-owned and regenerable; `## My notes` below is yours.

See [`../docs/architecture.md`](../docs/architecture.md) and [`../CONTEXT.md`](../CONTEXT.md).
