# Skills

Claude Code skills for the research vault. Planned: `lit-sync` (Layer 1), `topic-cluster` (Layer 2),
`promote-idea` (2→3), `project-status` (Layer 3), `feasibility` (deferred stub).

Mechanical I/O lives in deterministic helper scripts; bulk LLM work fans out via the Workflow tool.
See [`../../docs/architecture.md`](../../docs/architecture.md).
