# .research — machine state

Internal state for the skills (hidden from Obsidian via the dot-prefix). Holds `manifest.json`
(per-citekey content hashes for sync change-detection) and sync logs. Transient subpaths are
gitignored; the manifest itself is tracked.
